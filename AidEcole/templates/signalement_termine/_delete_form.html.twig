<form method="post" action="{{ path('app_signalement_termine_delete', {'id': signalement_termine.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ signalement_termine.id) }}">
    <button class="btn">Delete</button>
</form>
