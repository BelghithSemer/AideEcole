<form method="post" action="{{ path('app_paiement_annonce_delete', {'id': paiement_annonce.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ paiement_annonce.id) }}">
    <button class="btn">Delete</button>
</form>
