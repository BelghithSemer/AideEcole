<form method="post" action="{{ path('app_favoris_delete', {'id': favoris.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ favoris.id) }}">
    <button class="btn">Delete</button>
</form>
