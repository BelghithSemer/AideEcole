{% extends 'base.html.twig' %}

{% block title %}CommentaireForum{% endblock %}

{% block body %}
    <h1>CommentaireForum</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commentaire_forum.id }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ commentaire_forum.Description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_commentaire_forum_index') }}">back to list</a>

    <a href="{{ path('app_commentaire_forum_edit', {'id': commentaire_forum.id}) }}">edit</a>

    {{ include('commentaire_forum/_delete_form.html.twig') }}
{% endblock %}
